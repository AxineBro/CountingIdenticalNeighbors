# Vector Processor Program

Программа для работы с векторами различных типов данных с использованием `std::variant`. Позволяет создавать, заполнять и анализировать векторы, подсчитывая количество элементов с одинаковыми соседями.

## 🚀 Функциональность

- **Поддержка множества типов данных**: int, long, double, float, char, std::string
- **Интерактивный ввод**: пользовательский интерфейс для выбора типа и ввода данных
- **Защищенный ввод**: валидация и обработка ошибок ввода
- **Анализ данных**: подсчет элементов с одинаковыми соседями
- **Кроссплатформенность**: использует стандартную библиотеку C++

## 📊 Поддерживаемые типы данных

1. **int** - целые числа
2. **long** - длинные целые числа  
3. **double** - числа двойной точности
4. **float** - числа одинарной точности
5. **char** - символы
6. **std::string** - строки

## ⚙️ Требования

- **Компилятор**: поддерживающий C++20
- **Стандартная библиотека**: STL
- **Система**: любая совместимая с C++20

## 🔨 Сборка и запуск

### Компиляция с GCC:

```bash
g++ -std=c++20 -o vector_processor Main.cpp
```

### Компиляция с Clang:

```bash
clang++ -std=c++20 -o vector_processor Main.cpp
```

### Запуск:

```bash
./vector_processor
```

## 🎮 Использование

### Пример сеанса работы:

```
Select the type of vector:
1. int
2. long
3. double
4. float
5. char
6. std::string
Enter choice: 1

Enter the number of elements: 5
Enter 5 values:
Value 1: 10
Value 2: 20
Value 3: 10
Value 4: 30
Value 5: 40

The elements with indexes 1 (10) and 3 (10) are the same
Identical neighbors: 1
```

### Шаги работы:

1. **Выбор типа данных** - пользователь выбирает тип элементов вектора
2. **Ввод размера** - указывается количество элементов (положительное число)
3. **Заполнение данными** - последовательный ввод значений
4. **Обработка и вывод** - программа анализирует вектор и выводит результаты

## 🔍 Алгоритм работы

Программа подсчитывает количество элементов, у которых **левый и правый соседи равны**.

Для вектора `[A, B, C, D, E]` анализируются:
- Элемент `B`: сравниваются `A` и `C`
- Элемент `C`: сравниваются `B` и `D`
- Элемент `D`: сравниваются `C` и `E`

**Условия**:
- Требуется минимум 3 элемента
- Для каждого элемента (кроме первого и последнего) проверяются соседи
- Выводится информация о найденных совпадениях и общее количество

### Пример:

Вектор: `[1, 2, 1, 3, 4]`
- Для элемента с индексом 1 (значение 2): соседи `1` и `1` → **совпадают**
- Для элемента с индексом 2 (значение 1): соседи `2` и `3` → разные
- Для элемента с индексом 3 (значение 3): соседи `1` и `4` → разные

**Результат**: 1 совпадение

## 📁 Структура проекта

### Основные компоненты:

- **Класс Main** - основной класс приложения
- **VectorVariant** - тип-вариант для хранения векторов разных типов
- **Методы**:
  - `main()` - точка входа в программу
  - `choiseVector()` - выбор типа вектора
  - `fillArray()` - заполнение вектора данными
  - `processProgram()` - обработка и анализ данных
  - `inputNumber()` - защищенный ввод чисел

### Особенности реализации:

- **Использование std::variant** для типобезопасной работы с разными типами
- **Шаблонные лямбды** с `std::visit` для обработки вариантов
- **Защита от некорректного ввода** с очисткой буфера
- **Специальная обработка** для строк и символов

## 🛠 Технические детали

### Обработка ввода:

- **Числа**: проверка формата и положительности
- **Строки**: специальная обработка для корректного ввода
- **Символы**: очистка буфера после ввода

### Обработка ошибок:

- Некорректный выбор типа → выбор по умолчанию (std::string)
- Неверный формат числа → повторный запрос
- Недостаточно элементов → информационное сообщение
